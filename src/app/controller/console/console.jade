div(style='position: fixed; top: 0; left: 0; right: 0;')
    md-toolbar.md-toolbar-tools
        h2
            span(ng-bind='vm.$amaotoCore.localData.serverInfo.siteName')
        small
            span &nbsp;Ver.
            span(ng-bind='vm.$amaotoCore.localData.serverInfo.version')
        md-button(ng-click='vm.$state.go("console.user.list")')
            md-icon account_box
            |  用户
        md-button(ng-click='vm.$state.go("console.file.list")')
            md-icon account_box
            |  文件
        span(flex)
        amaoto-user-menu

div(style='height: 64px;')

div(layout="vertical", layout-fill)
    div(ui-view='console.frame', layout-fill)